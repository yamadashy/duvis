[package]
name = "duvis"
version = "0.1.0"
edition = "2021"
description = "Disk usage visualizer for both AI and humans"
repository = "https://github.com/yamadashy/duvis"
homepage = "https://github.com/yamadashy/duvis"
documentation = "https://docs.rs/duvis"
readme = "README.md"
license = "MIT"
authors = ["Kazuki Yamada <koukun0120@gmail.com>"]
keywords = ["disk-usage", "cli", "treemap", "visualizer", "ai"]
categories = ["command-line-utilities", "filesystem", "visualization"]
build = "build.rs"
# Explicit allow-list of files shipped to crates.io. `src/ui/index.html` is
# generated by build.rs from ui/src/ and gitignored, so we list it here so it
# rides along in the published tarball (end users do not have Node).
# Patterns are gitignore-style; the leading "/" anchors them to the package
# root so we don't accidentally pick up matching files deep in ui/node_modules.
include = [
    "/Cargo.toml",
    "/Cargo.lock",
    "/README.md",
    "/LICENSE",
    "/build.rs",
    "/src/**/*.rs",
    "/src/ui/index.html",
]

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
rayon = "1"
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
open = "5"

[dev-dependencies]
assert_cmd = "2"
http-body-util = "0.1"
insta = { version = "1", features = ["filters"] }
predicates = "3"
tempfile = "3"
tower = { version = "0.5", features = ["util"] }
